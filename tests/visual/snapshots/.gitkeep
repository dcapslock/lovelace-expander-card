# Visual test baseline snapshots.
# PNG baselines are committed here (generated by ha_testcontainer on first run
# or when SNAPSHOT_UPDATE=1 is set).
# Actual screenshots (*.actual.png) are gitignored.
