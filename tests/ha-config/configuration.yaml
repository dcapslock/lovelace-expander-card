homeassistant:
  country: US
  unit_system: metric
  time_zone: UTC
  customize: !include customize.yaml

default_config:

demo:

lovelace:
  mode: storage
  resource_mode: yaml
  resources: !include lovelace_resources.yaml
